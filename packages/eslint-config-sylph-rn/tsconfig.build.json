{
  "extends": "../../tsconfig.json", // Inherit from workspace root tsconfig
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "noEmit": false,
    "jsx": "react-jsx",
    "typeRoots": ["./src/types", "../../node_modules/@types"],
    "composite": true // Enable project references
  },
  "references": [ // RN depends on React (which depends on Base)
    { "path": "../react/tsconfig.build.json" }
    // No need to directly reference base, TS follows the chain
  ],
  "include": ["src/**/*.ts", "src/**/*.d.ts"],
  "exclude": ["node_modules", "dist"]
}